# Project Management System
### _Get started :)_
- ##### No setup required, just clone the repo and start the server and App is ready to be used.
- ##### Simple data store that saves the data in JSON format to a single file(not use database).
- ##### Front-end developed with React. In the project directory, you can run: `npm install`.
 
